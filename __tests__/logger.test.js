'use strict';

// const logger = require('../src/cache-manager/logger.js');

describe('logger testing', () => {
  it('should take in a message and console.log it if a save', () => {
  
  });


  it('should take in a message and console.error it if an error', () => {

  });
});